import React from 'react';
import ReactForms from 'react-forms';
const {schema, Form} = ReactForms;
const {Schema, Property} = schema;

export default class BaseForm extends {
  render() {
    return (
      <div />
    );
  }
};
